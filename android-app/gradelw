#!/usr/bin/env bash
# Convenience shim for common typo: `gradelw` -> `gradlew`
# Forwards all arguments to the Gradle wrapper.

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/gradlew" "$@"

